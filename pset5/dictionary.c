/**
 * dictionary.c
 *
 * Computer Science 50
 * Problem Set 5
 *
 * Implements a dictionary's functionality.
 */

#include <stdbool.h>

#include "dictionary.h"

/**
 * Returns true if word is in dictionary else false. - Возвращает true, если слово находится в словаре иначе false
 */
bool check(const char* word)
{
    // TODO
    return false;
}

/**
 * Loads dictionary into memory.  Returns true if successful else false. Загружает словарь в память. Возвращает true, если в противном случае false.
 */
bool load(const char* dictionary)
{

    return false;
}

/**
 * Returns number of words in dictionary if loaded else 0 if not yet loaded. - Возвращает количество слов в словаре, если загружено else 0, если еще не загружено
 */
unsigned int size(void)
{
    // TODO
    return 0;
}

/**
 * Unloads dictionary from memory.  Returns true if successful else false. - Выгружает словарь из памяти. Возвращает true, если успешно else false
 */
bool unload(void)
{
    // TODO
    return false;
}
